# Build output.
/public/

# Zola's resized-image cache. Older Zola versions wrote generated variants
# here rather than straight into public/; ignored so neither layout can leak
# generated images into the repo.
/static/processed_images/

# Generated by the deploy workflow from base_url. Not committed, so the domain
# lives in zola.toml and nowhere else. See .github/workflows/deploy.yml.
/static/CNAME

# OS noise.
.DS_Store
Thumbs.db
desktop.ini
