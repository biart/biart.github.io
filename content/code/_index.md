+++
title = "Code"
description = "Graphics, C++, and things that compile."

# Newest first, same as the front page.
sort_by = "date"

# Hands this section's pages up to the root section so they appear on the front
# page. It does NOT empty this section: section.pages still lists exactly these
# posts, and the feed below still covers only this section.
transparent = true

# THE PER-SECTION FEED. Note the plural: this key was `generate_feed` before
# Zola 0.19. Produces /code/atom.xml (the filename comes from feed_filenames in
# zola.toml, so switching to RSS is a one-line change there).
generate_feeds = true
+++
